 apiVersion: extensions/v1beta1
 kind: Deployment
 metadata:
   name: nullschool-dpt-dev
   namespace: develop
 spec:
   replicas: 1
   template:
     metadata:
       labels:
         app: nullschool-app
     spec:
       containers:
       - name: nullschool-con
         image: dkhub.funceme.br:444/web/nullschool:develop
         imagePullPolicy: Always
         ports:
         - name: api-port80
           containerPort: 80
       nodeSelector:
         env: develop
       imagePullSecrets:
       - name: dkhub.funceme.br-registry-secret

---
 apiVersion: v1
 kind: Service
 metadata:
   name: svc-nullschool
 spec:
   selector:
     app: nullschool-app
   ports:
     - name: api-port80
       protocol: TCP
       port: 80
       targetPort: 80
